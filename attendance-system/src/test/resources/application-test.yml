spring:
  datasource:
    # Overridden dynamically by @DynamicPropertySource in base test class
    url: jdbc:tc:postgresql:16:///attendance_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration

  mail:
    host: localhost
    port: 3025
    username: test@auca.ac.rw
    password: test
    properties:
      mail.smtp.auth: false
      mail.smtp.starttls.enable: false

application:
  security:
    jwt:
      secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      expiration: 3600000         # 1 hour for tests
      refresh-token:
        expiration: 86400000      # 1 day for tests
